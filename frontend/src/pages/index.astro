---
// Energy Dashboard DS3 - Home Page
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>Energy Dashboard DS3 | Center for Energy Research</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar">
      <div class="nav-brand">
        <span class="brand-icon">⚡</span>
        <span class="brand-text">ENERGY<span class="brand-accent">DS3</span></span>
      </div>
      <div class="nav-links">
        <a href="/" class="active">Home</a>
        <a href="/global-targets">Global Targets</a>
        <a href="/energy-access">Energy Access</a>
        <a href="#about">About</a>
      </div>
    </nav>

    <header class="hero">
      <div class="hero-content">
        <p class="hero-label">Welcome to</p>
        <h1 class="hero-title">
          Energy Dashboard
          <span class="title-highlight">DS3</span>
        </h1>
        <p class="hero-description">
          Comprehensive analysis of global renewable energy targets and US state-level 
          energy access, affordability, and reliability metrics.
        </p>
        <div class="hero-actions">
          <a href="/global-targets" class="btn btn-primary">
            <span class="btn-icon">🌍</span>
            <span>Global Targets</span>
          </a>
          <a href="/energy-access" class="btn btn-secondary">
            <span class="btn-icon">⚡</span>
            <span>US Energy Access</span>
          </a>
        </div>
      </div>
      
      <div class="hero-visual">
        <div class="visual-card">
          <div class="visual-number">88</div>
          <div class="visual-label">Countries Tracked</div>
        </div>
        <div class="visual-card">
          <div class="visual-number">51</div>
          <div class="visual-label">US States & DC</div>
        </div>
        <div class="visual-card">
          <div class="visual-number">2030</div>
          <div class="visual-label">Target Year</div>
        </div>
      </div>
    </header>

    <section id="features" class="section">
      <div class="section-header">
        <h2>Explore Our Dashboards</h2>
        <p>Dive into comprehensive energy data and insights</p>
      </div>
      
      <div class="features-grid">
        <a href="/global-targets" class="feature-card">
          <div class="feature-icon">🌍</div>
          <h3>Global Renewable Targets</h3>
          <p>
            Track national commitments to renewable energy capacity across 88 countries. 
            Analyze 2030 targets, regional breakdowns, and technology mix.
          </p>
          <div class="feature-stats">
            <span class="stat-item">
              <strong>86</strong> Capacity Targets
            </span>
            <span class="stat-item">
              <strong>66</strong> Share Targets
            </span>
          </div>
          <span class="feature-link">Explore Global Data →</span>
        </a>
        
        <a href="/energy-access" class="feature-card">
          <div class="feature-icon">⚡</div>
          <h3>US Energy Access</h3>
          <p>
            Examine electricity affordability, grid reliability, and energy burden across 
            all 50 US states and Washington DC.
          </p>
          <div class="feature-stats">
            <span class="stat-item">
              <strong>51</strong> States Analyzed
            </span>
            <span class="stat-item">
              <strong>24</strong> Years of Data
            </span>
          </div>
          <span class="feature-link">Explore US Data →</span>
        </a>
      </div>
    </section>

    <section id="highlights" class="section section-alt">
      <div class="section-header">
        <h2>Key Insights</h2>
        <p>Notable findings from our analysis</p>
      </div>
      
      <div class="highlights-grid">
        <div class="highlight-card">
          <div class="highlight-icon">🌍</div>
          <h4>Europe Leads Global Commitments</h4>
          <p>41 European countries have set renewable energy targets, representing 47% of all tracked nations.</p>
        </div>
        
        <div class="highlight-card">
          <div class="highlight-icon">💰</div>
          <h4>Energy Burden Disparities</h4>
          <p>Mississippi households spend 3.17% of income on electricity—nearly 3x Utah's 1.17% rate.</p>
        </div>
        
        <div class="highlight-card">
          <div class="highlight-icon">⚡</div>
          <h4>Grid Reliability Gap</h4>
          <p>West Virginia experiences 486 minutes of outages annually—18x more than DC's 26 minutes.</p>
        </div>
        
        <div class="highlight-card">
          <div class="highlight-icon">☀️</div>
          <h4>Solar Dominance</h4>
          <p>Solar capacity targets exceed wind in the majority of 2030 national commitments worldwide.</p>
        </div>
      </div>
    </section>

    <section id="about" class="section">
      <div class="section-content-wide">
        <h2>About This Project</h2>
        <p class="about-intro">
          Energy Dashboard DS3 is a comprehensive data visualization platform developed by the 
          Center for Energy Research to track and analyze global renewable energy commitments 
          and US energy access metrics.
        </p>
        
        <div class="about-grid">
          <div class="about-card">
            <h3>Data Sources</h3>
            <ul>
              <li>Ember Climate - Global 2030 Targets Dataset</li>
              <li>EIA (Energy Information Administration)</li>
              <li>US Census Bureau ACS 2024</li>
              <li>IRENA (International Renewable Energy Agency)</li>
            </ul>
          </div>
          
          <div class="about-card">
            <h3>Project Goals</h3>
            <ul>
              <li>Track progress toward SDG 7 (Affordable & Clean Energy)</li>
              <li>Identify energy equity challenges in the United States</li>
              <li>Provide accessible, interactive data visualizations</li>
              <li>Support evidence-based energy policy decisions</li>
            </ul>
          </div>
          
          <div class="about-card">
            <h3>Technologies</h3>
            <ul>
              <li>Astro - Static site framework</li>
              <li>React - Interactive components</li>
              <li>D3.js - Data visualization library</li>
              <li>Python - Data processing & analysis</li>
            </ul>
          </div>
        </div>
      </div>
    </section>

    <footer class="footer">
      <div class="footer-content">
        <div class="footer-brand">
          <span class="brand-icon">⚡</span>
          <span>ENERGY DS3</span>
        </div>
        <p>Center for Energy Research · Data Science Project</p>
        <p class="footer-source">Data: Ember Climate, EIA, Census Bureau ACS 2024</p>
      </div>
    </footer>
  </body>
</html>

<style>
  :root {
    --color-bg: #ffffff;
    --color-bg-alt: #f8faf9;
    --color-surface: #f1f5f3;
    --color-primary: #16a34a;
    --color-primary-dim: #22c55e;
    --color-accent: #15803d;
    --color-text: #1a1a1a;
    --color-text-muted: #6b7280;
    --color-border: #e5e7eb;
    --font-sans: 'DM Sans', system-ui, sans-serif;
    --font-mono: 'Space Mono', monospace;
  }

  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    font-family: var(--font-sans);
    background: var(--color-bg);
    color: var(--color-text);
    line-height: 1.6;
  }

  /* Navbar */
  .navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 1.25rem 3rem;
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--color-border);
    z-index: 100;
  }

  .nav-brand {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-weight: 700;
    font-size: 1.1rem;
    color: var(--color-text);
  }

  .brand-icon {
    font-size: 1.5rem;
  }

  .brand-accent {
    color: var(--color-primary);
  }

  .nav-links {
    display: flex;
    gap: 2.5rem;
  }

  .nav-links a {
    color: var(--color-text-muted);
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    transition: color 0.2s;
  }

  .nav-links a:hover,
  .nav-links a.active {
    color: var(--color-primary);
  }

  /* Hero */
  .hero {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 8rem 3rem 4rem;
    text-align: center;
    background: 
      radial-gradient(ellipse at 50% 50%, rgba(22, 163, 74, 0.08) 0%, transparent 60%),
      var(--color-bg);
  }

  .hero-content {
    max-width: 800px;
    margin-bottom: 4rem;
  }

  .hero-label {
    font-family: var(--font-mono);
    font-size: 0.85rem;
    color: var(--color-primary);
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 1rem;
  }

  .hero-title {
    font-size: 5rem;
    font-weight: 700;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    color: var(--color-text);
  }

  .title-highlight {
    display: block;
    color: var(--color-primary);
    font-family: var(--font-mono);
  }

  .hero-description {
    font-size: 1.3rem;
    color: var(--color-text-muted);
    max-width: 650px;
    margin: 0 auto 2.5rem;
    line-height: 1.7;
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
  }

  .btn {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    border-radius: 8px;
    transition: all 0.2s;
    font-family: var(--font-sans);
  }

  .btn-icon {
    font-size: 1.3rem;
  }

  .btn-primary {
    background: var(--color-primary);
    color: #ffffff;
    box-shadow: 0 4px 12px rgba(22, 163, 74, 0.2);
  }

  .btn-primary:hover {
    background: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(22, 163, 74, 0.3);
  }

  .btn-secondary {
    background: transparent;
    color: var(--color-text);
    border: 2px solid var(--color-border);
  }

  .btn-secondary:hover {
    border-color: var(--color-primary);
    color: var(--color-primary);
    transform: translateY(-2px);
  }

  /* Hero Visual */
  .hero-visual {
    display: flex;
    gap: 2rem;
    justify-content: center;
  }

  .visual-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 16px;
    padding: 2rem 2.5rem;
    min-width: 160px;
    text-align: center;
  }

  .visual-number {
    font-family: var(--font-mono);
    font-size: 3rem;
    font-weight: 700;
    color: var(--color-primary);
    margin-bottom: 0.5rem;
  }

  .visual-label {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  /* Sections */
  .section {
    padding: 5rem 3rem;
  }

  .section-alt {
    background: var(--color-bg-alt);
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-header h2 {
    font-size: 2.5rem;
    font-weight: 700;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .section-header p {
    font-size: 1.1rem;
    color: var(--color-text-muted);
  }

  /* Features Grid */
  .features-grid {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }

  .feature-card {
    background: var(--color-bg);
    border: 2px solid var(--color-border);
    border-radius: 16px;
    padding: 2.5rem;
    text-decoration: none;
    color: var(--color-text);
    transition: all 0.3s;
    display: flex;
    flex-direction: column;
  }

  .feature-card:hover {
    border-color: var(--color-primary);
    transform: translateY(-4px);
    box-shadow: 0 12px 28px rgba(22, 163, 74, 0.15);
  }

  .feature-icon {
    font-size: 3rem;
    margin-bottom: 1.5rem;
  }

  .feature-card h3 {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: var(--color-text);
  }

  .feature-card p {
    font-size: 1rem;
    color: var(--color-text-muted);
    margin-bottom: 1.5rem;
    line-height: 1.6;
    flex-grow: 1;
  }

  .feature-stats {
    display: flex;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--color-border);
  }

  .stat-item {
    font-size: 0.9rem;
    color: var(--color-text-muted);
  }

  .stat-item strong {
    font-family: var(--font-mono);
    font-size: 1.1rem;
    color: var(--color-primary);
    margin-right: 0.25rem;
  }

  .feature-link {
    font-family: var(--font-mono);
    font-size: 0.9rem;
    color: var(--color-primary);
    font-weight: 600;
  }

  .feature-card:hover .feature-link {
    color: var(--color-accent);
  }

  /* Highlights Grid */
  .highlights-grid {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
  }

  .highlight-card {
    background: var(--color-bg);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 1.5rem;
    text-align: center;
  }

  .highlight-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .highlight-card h4 {
    font-size: 1rem;
    margin-bottom: 0.75rem;
    color: var(--color-text);
  }

  .highlight-card p {
    font-size: 0.85rem;
    color: var(--color-text-muted);
    line-height: 1.5;
  }

  /* About Section */
  .section-content-wide {
    max-width: 1200px;
    margin: 0 auto;
  }

  .about-intro {
    font-size: 1.2rem;
    color: var(--color-text-muted);
    text-align: center;
    max-width: 800px;
    margin: 0 auto 3rem;
    line-height: 1.7;
  }

  .about-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .about-card {
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: 12px;
    padding: 2rem;
  }

  .about-card h3 {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    color: var(--color-primary);
  }

  .about-card ul {
    list-style: none;
  }

  .about-card li {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    padding: 0.5rem 0;
    padding-left: 1.5rem;
    position: relative;
  }

  .about-card li::before {
    content: "→";
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }

  /* Footer */
  .footer {
    padding: 3rem;
    border-top: 1px solid var(--color-border);
    text-align: center;
    background: var(--color-bg);
  }

  .footer-brand {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    font-family: var(--font-mono);
    font-weight: 700;
    margin-bottom: 1rem;
    color: var(--color-text);
  }

  .footer-content p {
    color: var(--color-text-muted);
    font-size: 0.9rem;
  }

  .footer-source {
    font-family: var(--font-mono);
    font-size: 0.8rem !important;
    margin-top: 0.5rem;
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .hero-title {
      font-size: 3.5rem;
    }
    
    .features-grid {
      grid-template-columns: 1fr;
    }
    
    .highlights-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .about-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 768px) {
    .navbar {
      padding: 1rem 1.5rem;
    }
    
    .nav-links {
      display: none;
    }
    
    .hero {
      padding: 6rem 1.5rem 3rem;
    }
    
    .hero-title {
      font-size: 2.5rem;
    }
    
    .hero-actions {
      flex-direction: column;
    }
    
    .btn {
      width: 100%;
      justify-content: center;
    }
    
    .hero-visual {
      flex-direction: column;
      gap: 1rem;
    }
    
    .visual-card {
      width: 100%;
    }
    
    .section {
      padding: 3rem 1.5rem;
    }
    
    .highlights-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
